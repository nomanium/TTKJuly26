const user = {
    username: "testuser",
    email: "test@test.com",
    role: "admin"
};
console.log(user["username"]); // Accessing the username property
console.log(user["email"]); // Accessing the email property
console.log(user["role"]); // Accessing the role property